<!-- What does this change, and why? Link the issue if there is one. -->

- [ ] `uv run pytest` passes (the suite is offline by design)
- [ ] `uv run ruff check .` is clean
- [ ] CHANGELOG.md entry added, if a user would notice the change
